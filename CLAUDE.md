# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current State

This repository is currently a placeholder. It contains only `README.md` (a stub) and no source code, build tooling, tests, or dependencies yet.

There is therefore no build, lint, or test command to run at this time. As the codebase is established, update this file with:

- Build, lint, and test commands (including how to run a single test)
- High-level architecture that spans multiple files

## Repository Conventions

- The default branch is `main`.
- Do not create a pull request unless explicitly asked.
